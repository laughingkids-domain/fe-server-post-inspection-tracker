query propertyCardQuery($id: String!) {
	property(id:$id) {
    id
    addressCoordinate {
      lat
      lon
    }
    status
    urlSlug
    address
    address
    carSpaces
    bathrooms
    bedrooms
    photos
  }
}