// post inspection tracker
const { user } = require("./user");
const { listing } = require("./listing");
const { users } = require("./users");
const { listings } = require("./listings");

module.exports = {
  listing,
  listings,
  user,
  users
};
